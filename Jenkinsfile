pipeline {
    

    stages {
        stage('without Docker') {
            steps {
                sh '''
                echo "without docker"
                ls -la
                touch container-no.txt
                
                '''
            }
        }
        
    }
}
